@inject NavigationManager NavigationManager

@code {
    [Inject]
    private IdentityRedirectManager RedirectManager { get; set; }

    protected override async Task OnInitializedAsync()
    {
        RedirectManager.RedirectTo("/Login");
    }
    // protected override void OnInitialized()
    // {
    //     NavigationManager.NavigateTo($"/courses/etudiant/decouvrir?returnUrl={Uri.EscapeDataString(NavigationManager.Uri)}", forceLoad: true);
    // }
}
